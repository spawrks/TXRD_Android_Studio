<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/totalTeamView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AllTeams"
    android:textAlignment="center"
    android:background="@color/black"
>

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/teamSelector"
        android:layout_centerHorizontal="true"
        android:textAlignment="center"
        android:entries="@array/choose_team"
        android:background="#777777"
        />


    <WebView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:id="@+id/teamHeaderView"
        android:layout_below="@id/teamSelector"
        android:layout_centerHorizontal="true"
        android:layout_margin="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rosterView"
        android:layout_below="@id/teamHeaderView"
        android:layout_centerHorizontal="true"
        android:layout_margin="8dp"
        />


</RelativeLayout>
